% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualizations.R
\name{plot_oc}
\alias{plot_oc}
\title{Plot Operating Characteristics (OC)}
\usage{
plot_oc(oc_data, x_var, facet_formula = NULL, plot_type = "stepwise", ...)
}
\arguments{
\item{oc_data}{A data frame containing the operating characteristics,
typically from \code{process_sim_results()}. Must contain columns for the
decision proportions (e.g., \code{proportion_pr}) and the decision types
(e.g., \code{decision_pr}).}

\item{x_var}{A character string specifying the variable for the x-axis
(e.g., \code{"control.n"} or \code{"control.p.diff"}).}

\item{facet_formula}{A formula for faceting the plot, to split it into
panels based on other setting variables (e.g., \code{~ borrowing} or
\code{true_value.compare_true ~ borrowing}).}

\item{plot_type}{A character string specifying the type of plot.
Either "stepwise" for a bar plot with text or "smooth" for a stacked area plot.
Defaults to "stepwise".}

\item{...}{Additional arguments passed to \code{ggplot2} functions for further
customization (e.g., \code{labs()}, \code{theme()}).}
}
\value{
A ggplot object.
}
\description{
Creates a stacked plot of the operating characteristics, showing the
probabilities of "Go", "No-Go", and "Consider" decisions for each
simulation scenario.
}
\examples{
# Assuming 'processed_results' is the output from process_sim_results()
# and contains an 'oc_all' data frame.

# plot_oc(
#   oc_data = processed_results$oc_all,
#   x_var = "control.n",
#   facet_formula = true_value.compare_true ~ borrowing,
#   labs(
#     title = "Operating Characteristics by Sample Size and Borrowing",
#     x = "Sample Size per Arm",
#     y = "Probability of Decision"
#   )
# )
}
