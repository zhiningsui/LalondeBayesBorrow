% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{lalonde_calibrate_no_conflict}
\alias{lalonde_calibrate_no_conflict}
\title{Calibrate Lalonde Go/No-Go Thresholds (one-time, no-conflict)}
\usage{
lalonde_calibrate_no_conflict(
  prior_params_list,
  lrv,
  tv,
  alpha_N = 0.1,
  alpha_G = 0.2,
  treatment_n = 45,
  control_n = 45,
  control_p = NULL,
  control_h_n = 180,
  control_h_p = 0.3,
  M_cal = 1e+05,
  save_path = NULL,
  verbose = TRUE
)
}
\arguments{
\item{prior_params_list}{A named \code{list} of prior/method specifications.
Each element is passed to \code{bayesian_lalonde_decision()} as \code{prior_params}.}

\item{lrv, tv}{Numeric. Minimal and target effect margins (on the \code{endpoint="binary"} scale).}

\item{alpha_N, alpha_G}{Target risks for FSR and FGR, respectively.}

\item{treatment_n, control_n}{Integers. Sample sizes for treatment and control in the calibration design.}

\item{control_p}{Numeric in [0,1]. Control response rate in the calibration design.}

\item{control_h_n}{Integer. Historical control sample size.}

\item{control_h_p}{Numeric in [0,1]. Historical control response rate.}

\item{M_cal}{Integer. Number of Monte Carlo replicates for stable percentiles.}

\item{save_path}{Optional file path (e.g., \code{"CALIBRATION_NO_CONFLICT.rds"}) to
save the returned object with \code{saveRDS}. If \code{NULL}, nothing is saved.}

\item{verbose}{Logical. Print progress.}
}
\value{
A list with:
\itemize{
\item \code{taus_by_method}: named list of \code{list(tau_TV, tau_LRV)} per method
\item \code{calib_table}: data.frame of calibrated thresholds and diagnostics
\item \code{lrv}, \code{tv}: the margins used
\item \code{note}: a short note about the setup
}
}
\description{
Runs a one-time calibration of (\eqn{\tau_{TV}}, \eqn{\tau_{LRV}}) across all
methods in \code{prior_params_list}, using a \emph{single} no-conflict design.
Internally:
\enumerate{
\item Simulates data under \eqn{\Delta=\mathrm{TV}} and \eqn{\Delta=\mathrm{LRV}}
for the same (t, c) design and a given historical control.
\item Computes posterior probabilities with \code{bayesian_lalonde_decision(..., posterior_infer=TRUE)}.
\item Calibrates \eqn{\tau_{TV}} (FSR control) and \eqn{\tau_{LRV}} (FGR control) via \code{calibrate_taus()}.
}
}
\details{
This performs \emph{one} calibration under no prior–data conflict and is intended to be
reused across other scenarios (e.g., with varying conflict levels) without re-calibration.
}
\examples{
\dontrun{
res <- lalonde_calibrate_no_conflict(
  prior_params_list = my_priors,
  lrv = 0.10, tv = 0.20,
  treatment_n = 45, control_n = 45, control_p = 0.30,
  control_h_n = 180, control_h_p = 0.30,
  M_cal = 5e4, save_path = "CALIBRATION_NO_CONFLICT.rds"
)
res$calib_table
}
}
\seealso{
\code{\link{bayesian_lalonde_decision}} (posterior inference) for binary endpoints,
\code{\link{data_gen_binary}} (simulation), \code{\link{create_data_gen_params}} (historical setup).
}
