% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{build_sam_args}
\alias{build_sam_args}
\title{Build argument list for a SAM posterior call}
\usage{
build_sam_args(
  arm,
  prior_params,
  arm_data_list,
  historical_data_list,
  endpoint,
  hist_suffix = "_h",
  validate = TRUE,
  require_current = FALSE,
  require_historical = FALSE
)
}
\arguments{
\item{arm}{Character arm name (e.g., \code{"control"} or \code{"treatment"}).}

\item{prior_params}{Named list of prior parameters (e.g., \code{control.a}, \code{control.b}, \code{control.w}, \code{control.delta_gate}, \code{control.delta_SAM}, \code{control.ess_h}, etc.).}

\item{arm_data_list}{List of arm data (expects elements named by arms).}

\item{historical_data_list}{List of historical data (expects elements named like \code{paste0(arm, hist_suffix)}).}

\item{endpoint}{Endpoint string passed through to \code{posterior_distribution}.}

\item{hist_suffix}{Suffix for historical arm names (default \code{"_h"}).}

\item{validate}{If \code{TRUE}, run basic checks.}

\item{require_current}{If \code{TRUE}, error if current data is missing.}

\item{require_historical}{If \code{TRUE}, error if historical data is missing.}
}
\value{
A named list suitable for \code{do.call(posterior_distribution, ...)}.
}
\description{
Collects current/historical data and SAM prior parameters for a given arm
(e.g., \code{"control"}, \code{"treatment"}), performs light validation,
and returns a list ready for \code{do.call(posterior_distribution, ...)}.
}
\examples{
# build_sam_args("control", prior_params, arm_data_list, historical_data_list, endpoint = "binary")
}
